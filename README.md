# AngularDock
Docker Angular production environment

Dependencies
----------------------
1. Angular-CLI
2. Docker Engine, Docker-Compose

Getting Start
----------------------
	ng new myproject
	npm install
	ng build --prod
	cd ./angulardock
	docker-compose up -d
